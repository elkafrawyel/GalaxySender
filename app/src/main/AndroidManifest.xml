<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.galaxycast.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:name=".GalaxyCastApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.galaxycast.app.ui.login.ActivationActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Fullscreen" />
        <activity
            android:name="com.galaxycast.app.ui.search.SearchActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.galaxycast.app.ui.login.CheckActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.galaxycast.app.ui.player.PlayerActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Fullscreen" />

        <activity
            android:name="com.galaxycast.app.ui.expandedcontrols.ExpandedControlsActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.CastVideosDark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.galaxycast.app.ui.search.SearchActivity" />
        </activity>

        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.galaxycast.app.utily.CastOptionsProvider" />
    </application>

</manifest>